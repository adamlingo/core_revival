<%= form_for(@benefit_profile) do |f| %>
  <% if @benefit_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@benefit_profile.errors.count, "error") %> prohibited this benefit_profile from being saved:</h2>

      <ul>
      <% @benefit_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.number_field :company_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
