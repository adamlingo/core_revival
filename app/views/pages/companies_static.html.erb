<% provide(:title, 'Companies') %>
<h1></br>Companies</h1>

<h2>
  List of Companies currently on platform:
</h2>

<h3>
<table>
  <tbody>
      <!-- Temp body removed -->
      <% @companies.each do |co| %>
      <tr>
        <td><%= co.name %></td>
        <td><%= co.email %></td>
      </tr>
      <% end %>
  </tbody>
</table>
</h3>
