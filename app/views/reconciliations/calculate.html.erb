<p id="notice"><%= notice %></p>

<h1>Reconciliation Calculation</h1>
</br>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @reconciliations.each do |r| %>
      <%= r %>
      </br>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back to Reconciliations', company_index_path(company_id: @helper.company_id, month: @helper.month, year: @helper.year) %>
